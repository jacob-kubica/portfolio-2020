import { SecuritiesExchange } from './securities-exchange.model';

describe('SecuritiesExchange', () => {
  it('should create an instance', () => {
    expect(new SecuritiesExchange()).toBeTruthy();
  });
});
